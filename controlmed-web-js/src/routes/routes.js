export default Object.freeze({
    // Public
    LOGIN: '/login',
    REGISTER: '/register',

    // Private
    DASHBOARD: '/home',
    DEPENDENTS: '/dependents',
    RECORDS: '/records',
    PROFILE: '/profile'
});
