
import { Typography, Card, CardContent } from '@mui/material';

export default function Dashboard() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h5">Dashboard</Typography>
        <Typography>Resumen general de ControlMed</Typography>
      </CardContent>
    </Card>
  );
}
